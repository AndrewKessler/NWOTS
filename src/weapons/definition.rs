#[derive(Clone)]
pub struct WeaponDefinition {

    pub id:
        String,

    pub display_name:
        String,

    pub ammo_per_pickup:
        u32,

    pub inventory_icon:
        String,

    pub viewmodel:
        String,
}