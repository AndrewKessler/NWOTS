#[derive(Clone)]
pub struct WeaponDefinition {

    pub id:
        String,

    pub display_name:
        String,

    pub pickup_ammo:
        u32,

    pub icon:
        String,

    pub viewmodel:
        String,
}